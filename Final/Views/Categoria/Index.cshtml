@model List<Final.Models.Categoria>
@{
    ViewData["Title"] = "Lista de categorías";
}

<div class="container-lg col-4">
    <h1>Todas las categorias</h1>
    @if (Model.Count() > 0)
    {
        <table class="table table-bordered" id="" width="100%" cellspacing="0">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Nombre</th>
                    <th>Fecha Alta</th>
                    <th>Activo</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var categoria in Model)
                {
                    <tr>
                        <td>@categoria.Id</td>
                        <td>@categoria.Nombre</td>
                        <td>@categoria.FechaAlta</td>
                        <td>@categoria.Activo</td>
                        <td>
                            <a asp-action="EditarCategoria" asp-route-id="@categoria.Id">Editar</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>No hay registros</p>
    }
</div>
